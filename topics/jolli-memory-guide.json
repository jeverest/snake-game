{
	"schemaVersion": 1,
	"stableSlug": "jolli-memory-guide",
	"title": "Jolli Memory Viewing Guide",
	"content": "# Jolli Memory Viewing Guide\n\nThis guide walks you through cloning the public `snake-game` repo, putting its Jolli Memory history in place, installing the tools, and viewing all of the memories inside VS Code or a JetBrains IDE.\n\nJolli Memory stores AI-generated commit summaries on a dedicated **git orphan branch** (`jollimemory/summaries/v3`) that ships with the repo. Nothing is checked out into your working tree — the branch just needs to exist locally so the CLI and IDE plugin can read it.\n\n![How Jolli Memory works: each commit is distilled by git and agent hooks into a structured summary stored on the jollimemory/summaries/v3 orphan branch, which produces a browsable Knowledge Wiki and an interactive Knowledge Graph you can recall and search across branches and machines.](docs/jolli-memory/how-it-works.svg)\n\n---\n\n## Prerequisites\n\n- **Node.js 22+** and npm. Check with `node -v`.\n- **git**\n- **VS Code** or a **JetBrains IDE** (e.g. IntelliJ IDEA). Screenshots below use VS Code; the JetBrains plugin has the same panel, wiki, and graph features.\n\n---\n\n## 1. Clone (or fork) the repo\n\n```bash\ngit clone https://github.com/jolliai/snake-game.git\ncd snake-game\n```\n\n> Prefer a fork? Fork `jolliai/snake-game` on GitHub first, then clone your fork. A normal `git clone` fetches **all** branches — including the Jolli Memory orphan branch — so no extra flags are needed.\n\n## 2. Put the Jolli Memory orphan branch in place\n\nThe memory lives on the `jollimemory/summaries/v3` branch. After cloning it exists as a remote-tracking ref (`origin/jollimemory/summaries/v3`); create a local branch from it so the tools can read it:\n\n```bash\ngit branch jollimemory/summaries/v3 origin/jollimemory/summaries/v3\n```\n\n**Do not check it out** — leave it as a branch. Your working directory stays on `main`. Confirm it's there:\n\n```bash\ngit branch --list 'jollimemory/*'\n#   jollimemory/summaries/v3\n```\n\n## 3. Install the Jolli CLI\n\n```bash\nnpm install -g @jolli.ai/cli\n```\n\nVerify:\n\n```bash\njolli --version\n```\n\n## 4. Sign in\n\n```bash\njolli auth login\n```\n\n> ⚠️ The sign-in command is `jolli auth login` — bare `jolli auth` is not a runnable command.\n\nFollow the browser prompt to authenticate, then confirm:\n\n```bash\njolli auth status\n#   Jolli Account:  Signed in\n#   Jolli API Key:  Configured\n```\n\nSigning in with your Jolli account powers AI generation — no separate Anthropic API key is required.\n\n## 5. Enable Jolli Memory in the repo\n\nFrom inside the `snake-game` folder:\n\n```bash\njolli enable\n```\n\nThis installs the lightweight git + AI-agent hooks and wires up the local orphan branch. Confirm everything is connected:\n\n```bash\njolli status\n```\n\nExpected output:\n\n```\n  Jolli Account:    Signed in\n  Stored memories:  82\n  Orphan branch:    jollimemory/summaries/v3\n```\n\nIf `Stored memories` is `0` or the orphan branch is missing, re-run step 2 and then `jolli doctor --fix`.\n\n> **Optional terminal checks:** `jolli view` lists commit summaries; `jolli recall` shows recalled context for the current branch.\n\n## 6. Install the IDE plugin\n\n**VS Code:**\n1. Open the **Extensions** view (`Cmd+Shift+X`).\n2. Search for **\"Jolli Memory\"** (publisher `jolli`, id `jolli.jollimemory-vscode`) and click **Install**.\n\n**JetBrains (IntelliJ IDEA, etc.):**\nGo to **Settings → Plugins → Marketplace**, search \"Jolli Memory\", and install. The JetBrains plugin manages its own git hooks — if you already ran `jolli enable` from the CLI (step 5), use `jolli enable --integrations-only` to avoid duplicating hooks.\n\n## 7. View the memories\n\n1. Open the `snake-game` folder in VS Code (**File → Open Folder…**).\n2. Click the **Jolli Memory** icon in the Activity Bar (left sidebar).\n3. The panel lists every stored memory — commit summaries with intent, decisions, and affected files. Use the search box to filter and **Load More** to page through older entries.\n4. Click any entry to expand it and see that commit's structured summary: **intent**, **decisions**, and **files** touched.\n\nIf the panel is empty, click **Refresh Memories** (or **Enable Jolli Memory** from the panel) and confirm the local `jollimemory/summaries/v3` branch from step 2 is present.\n\n---\n\n## What to do next\n\n### Build the Knowledge Wiki\n\nThe wiki distills per-commit memories into browsable **topic pages** (e.g. \"Iron Snake Mode\", \"Per-Mode Leaderboards\", \"Bot Strategies\") under a `snake-game · Knowledge Wiki` index. Building it requires two things:\n\n**a. Set a Memory Bank folder** — a local folder where Jolli writes the wiki and a Markdown copy of every memory. One folder can hold several repos; each gets its own subfolder (here, `snake-game/`, with the wiki under `snake-game/_wiki/`). Use an **absolute** path:\n\n```bash\nmkdir -p \"$HOME/jolli-memory-bank\"\njolli configure --set localFolder=\"$HOME/jolli-memory-bank\"\n```\n\n> **Prefer the IDE?** Open the Jolli Memory panel → **Settings** (gear icon) → **Memory Bank** → **Browse…** to pick or create the folder. An existing Obsidian vault works fine.\n\n> ⚠️ **Build Knowledge Wiki silently aborts if no Memory Bank folder is configured.** Set the folder first.\n\n**b. Confirm a model is configured.** Your Jolli account from step 4 is sufficient — nothing more to do. To use your own Anthropic key instead:\n\n```bash\njolli configure --set apiKey=sk-ant-...\njolli configure --set aiProvider=anthropic\n```\n\nKeys are stored locally as secrets and are never written into the repo. A shell `ANTHROPIC_API_KEY` env var also works.\n\n**c. Build:** From the **Build Knowledge Wiki** action in the panel, the Command Palette (`Cmd+Shift+P` → **Jolli Memory: Build Knowledge Wiki**), or the terminal:\n\n```bash\njolli compile\n```\n\n### View the wiki\n\nThe wiki is plain Markdown inside your Memory Bank folder, under `<memory-bank>/snake-game/_wiki/`. Open **`_wiki/_index.md`** for the topic index and click through to individual `topic--*.md` pages. In VS Code, `Cmd+Shift+V` opens a rendered Markdown preview.\n\n![The generated Knowledge Wiki: the _index.md topic index on the left links to individual topic pages like \"Per-Mode Leaderboards with Stats\" on the right.](docs/jolli-memory/knowledge-wiki.png)\n\n*The `_index.md` topic index (left) and one topic page (right) — real output for this repo.*\n\n### View the Knowledge Graph\n\nOpen the **View Knowledge Graph** action from the **Jolli Memory** panel — it opens an interactive graph (topics as nodes, related work linked) inside VS Code.\n\n> ⚠️ **\"View Knowledge Graph\" does not appear in the Command Palette** — it is only accessible from the panel.\n\n![Knowledge Graph overview: category cards (AI & Bot Behavior, Gameplay Features, Testing & CI, Project Documentation, UI & User Experience, Developer Tooling) with a stats sidebar showing 6 categories, 7 topics, 36 units.](docs/jolli-memory/knowledge-graph-overview.png)\n\n*Overview: each card is a category. Click one to drill into its topics and units.*\n\n![Knowledge Graph drilled into Gameplay Features: topic groups with unit cards tagged MECHANISM, DECISION, and FIX, connected by labeled edges such as \"caused-by\", \"extends\", and \"related-to\".](docs/jolli-memory/knowledge-graph-topics.png)\n\n*Drilling into a category reveals its topics, units, and how they relate.*\n\nTo export a self-contained HTML graph or open it in a browser, use the terminal. `--export <dir>` is required; `--open` is an optional modifier:\n\n```bash\njolli graph --export ./graph           # write ./graph/<repo>-graph.html\njolli graph --export ./graph --open    # …and open it in your browser\n```\n\n> ⚠️ `jolli graph --open` alone (without `--export`) errors. `--open` is a modifier to `--export`, not a standalone action.\n\n---\n\n## Files in this guide\n\n| File | Description |\n|---|---|\n| `docs/jolli-memory/README-JOLLI-MEMORY.md` | The full step-by-step guide |\n| `docs/jolli-memory/how-it-works.svg` | Architecture diagram: Commit → Distill → Store → Wiki/Graph pipeline |\n| `docs/jolli-memory/knowledge-wiki.png` | Screenshot: wiki index and a topic page |\n| `docs/jolli-memory/knowledge-graph-overview.png` | Screenshot: graph at category level |\n| `docs/jolli-memory/knowledge-graph-topics.png` | Screenshot: graph drilled into Gameplay Features |\n\nScreenshots were generated using Playwright against real repo data (not mockups), ensuring they accurately reflect actual tool output.\n\n---\n\n## Troubleshooting\n\n| Symptom | Fix |\n|---|---|\n| Panel or `jolli status` shows 0 memories | Re-run step 2 to create the local orphan branch, then `jolli doctor --fix`. |\n| `jolli: command not found` | Re-run `npm install -g @jolli.ai/cli`; confirm your npm global bin is on `PATH`. |\n| \"Not signed in\" | Run `jolli auth login` and verify with `jolli auth status`. |\n| **Build Knowledge Wiki** errors or does nothing | Confirm you're signed in (step 4) and a Memory Bank folder is set (`jolli configure --set localFolder=…`), then rebuild. |\n| Wiki or graph looks stale after new commits | Re-run **Build Knowledge Wiki** (`jolli compile`) — it regenerates topics from the latest memories. |\n| VS Code panel not appearing | Confirm the Jolli Memory extension is installed and enabled, then reload the window. |\n| `jolli graph --open` errors | Use `jolli graph --export <dir>` (required); append `--open` to also launch in browser. |",
	"relatedBranches": [
		"docs/jolli-memory-guide"
	],
	"sourceRefs": [
		{
			"type": "summary",
			"id": "39cd827d6568fd3b3de6cc7fb74bfa728fb845e3",
			"timestamp": "2026-07-09T21:48:27.000Z",
			"branch": "docs/jolli-memory-guide"
		},
		{
			"type": "plan",
			"id": "README-JOLLI-MEMORY-39cd827d",
			"timestamp": "2026-07-09T21:48:55.202Z",
			"branch": "docs/jolli-memory-guide"
		},
		{
			"type": "summary",
			"id": "96ca49848e5ce24ab4ad704a4eba3ecfe41ce4ea",
			"timestamp": "2026-07-09T21:48:27.000Z",
			"branch": "docs/jolli-memory-guide"
		}
	],
	"lastUpdatedAt": "2026-07-09T22:03:05.817Z"
}