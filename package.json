{
  "name": "snake",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "tangle": "node scripts/tangle.mjs",
    "dev": "node scripts/tangle.mjs && vite",
    "build": "node scripts/tangle.mjs && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.1.7"
  }
}
